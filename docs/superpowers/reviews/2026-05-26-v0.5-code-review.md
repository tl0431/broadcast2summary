# v0.5 Code Review — RSS Rich Metadata

**Date:** 2026-05-26  
**Branch:** `feat/v0.5-rss-rich-metadata`  
**Reviewer:** Agent (structured review per superpowers:requesting-code-review)  
**Verdict:** **APPROVED** — merge-ready after test run

## Strengths

1. **Clear data flow:** `rss.Episode` → `pipeline` → `summarize` / `output_*` with defaults preserving backward compatibility.
2. **Consistent failure mode:** Cover, wiki tags, and existing IM/wiki pushes all soft-fail without blocking `success`.
3. **Test coverage:** New unit tests for RSS, prompts, pipeline spy, outputs, wiki; e2e smoke asserts frontmatter/subtitle.
4. **No new dependencies:** HTML via stdlib; binary download reuses httpx patterns.
5. **Operational visibility:** Prompt size and truncation logs include episode guid after review fixes.

## Issues found and resolution

| Severity | Issue | Resolution |
|----------|-------|------------|
| Important | Cover skipped on transcript cache hit | Fixed: `_download_cover()` called after cache branch |
| Minor | Truncation/prompt WARN logs lacked guid | Fixed: `episode_guid` through prompts/summarize/pipeline |
| Minor | `pipeline` imports `_safe_filename` from `output_local` | Accepted; optional refactor to shared util later |
| Minor | `_download_binary_to_file` duplicates retry logic vs `download_audio` | Accepted per plan |
| Minor | Wiki tag probe lazy not at import | Accepted; matches plan implementation note |
| Minor | `push_wiki_tags` command placeholder until lark-cli verified | Accepted; probe gates execution |

## Security / secrets

- No credentials in new code paths.
- Cover URLs fetched from RSS only; same trust model as audio URL.

## Performance

- Shownotes capped at 1.5K — bounded prompt growth.
- Cover ~50–200KB per episode, once per guid (cached on disk).
- Map-reduce passes shownotes to each chunk — acceptable duplicate for noun anchoring.

## Files reviewed

- `src/broadcast2summary/rss.py`
- `src/broadcast2summary/download.py`
- `src/broadcast2summary/prompts.py`
- `src/broadcast2summary/summarize.py`
- `src/broadcast2summary/pipeline.py`
- `src/broadcast2summary/output_local.py`
- `src/broadcast2summary/output_im.py`
- `src/broadcast2summary/output_wiki.py`
- All associated `tests/*` changes + `tests/fixtures/sample_rich_feed.xml`

## Verification

```bash
pytest tests/ -m "not slow" -q
pytest tests/test_e2e_smoke.py -q
```

Expected: all pass.

## Sign-off

No Critical or open Important issues. Approved for merge to `main`.
